title: Collection with cursor-based pagination
type: object
description: Коллекция с пагинацией по курсору.
properties:
  cursors:
    type: object
    properties:
      self:
        type: string
        description: 'Cсылка или курсор в ответ на пагинацию или объект, указывающий на тот же объект или страницу коллекции .'
      first:
        type: string
        description: 'Ссылка или курсор в ответе на пагинацию или объект, указывающий на первый объект или страницу коллекции.'
      prev:
        type: string
        description: 'Ссылка или курсор в ответе на пагинацию или объект, указывающий на предыдущий объект или страницу коллекции.'
      next:
        type: string
        description: 'Ссылка или курсор в ответе на пагинацию или объект, указывающий на следующий объект или страницу коллекции.'
      last:
        type: string
        description: 'Ссылка или курсор в ответе на пагинацию или объект, указывающий на последний объект коллекции или страницу.'
      query:
        type: object
        description: 'Объект, содержащий фильтры запроса, применяемые в поисковом запросе для фильтрации ресурса коллекции.'
  items:
    type: array
    items: {}
